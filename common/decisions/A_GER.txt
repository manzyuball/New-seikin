A_GER_cat = { #ディシジョンカテゴリーの名前

	A_GER1941_1 = { #ディシジョンの名前

		icon = GFX_decision_steel  #ディシジョンのアイコン(gfx\interface\decisions\)
		available = {  #このディシジョンが選択できるようになる条件
			date > 1940.6.1
			date < 1941.6.1
		}

		visible = {  #このディシジョンが出現する条件
		    always = yes
		}

		cost = 1  #選択するのに必要な政治力
		fire_only_once = yes  #一度切りか否か

		complete_effect = { #選択時の効果
		add_resource = {
			type = aluminium
			amount = 100
			state = 52
		}
		add_resource = {
			type = steel
			amount = 100
			state = 42
		}
		add_resource = {
			type = chromium
			amount = 100
			state = 66
		}
		}
	}
	GER_barbarossa_declare_trigger = {
    activation = {
        NOT = { has_country_flag = GER_barbarossa_declare_done }
    }
    available = {
        always = no
    }
    cancel_trigger = {
        always = no
    }
    icon = mission_icon     # For GFX_decision_mission_icon
    is_good = no
    war_with_on_timeout = SOV
    days_mission_timeout = 172
    selectable_mission = yes
    timeout_effect = {
		set_country_flag = GER_barbarossa_declare_done
		country_event = { id = LR.4 }
    }
    }
}